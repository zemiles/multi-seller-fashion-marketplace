import {validateModel} from './schema-model.mjs';
import {applyTargetRules} from './target-rules.mjs';

// Documentation-only target overlay. Never execute as a migration.
export function targetModel(current){
 const m=structuredClone(current);m.changes=[];
 const serviceOf=ns=>m.tables.find(t=>t.id.startsWith(ns+'.')).service;
 const columns=raw=>raw.split(',').map(x=>{const [name,rawType]=x.trim().split(/\s+/);const nullable=rawType.endsWith('?'),type=rawType.replace(/[!?]$/,'');return {name,type,nullable,definition:`PLANNED ${name} ${type}${nullable?' NULL':' NOT NULL'}`,status:'planned'};});
 const add=(id,raw,pk,unique=[],refs=[],note='')=>{const [ns,name]=id.split('.'),service=serviceOf(ns);const t={id,name,service,schema:ns==='pg1'?'pgkakao':ns==='pg2'?'pgnaver':'marketplace',status:'planned',columns:columns(raw),pk:pk.split(','),unique:unique.map(k=>k.split(',')),constraints:[],indexes:[],note};m.tables.push(t);for(const [local,target,remote]of refs)m.fks.push({name:`${name}_${local.replaceAll(',','_')}_planned_fk`,from:id,columns:local.split(','),to:target,targetColumns:remote.split(','),sql:'PLANNED service-local FK',status:'planned'});m.changes.push({table:id,kind:'ADD_TABLE',note});return t;};
 const alter=(id,raw,note)=>{const t=m.tables.find(x=>x.id===id);if(!t)throw Error(id);for(const c of columns(raw)){const found=t.columns.find(x=>x.name===c.name);if(found){found.previousType=found.type;Object.assign(found,c);}else t.columns.push(c);}t.status='modified';t.note=(t.note||'')+' '+note;m.changes.push({table:id,kind:'ALTER_COLUMNS',columns:raw,note});};
 for(const ns of ['commerce','payment','settlement','discovery']){
  add(`${ns}.consumer_inbox`,'consumer_name text,event_id uuid,entry_id uuid,version bigint,aggregate_type text,aggregate_id text,aggregate_version bigint,payload jsonb,payload_hash text,status text,attempt_count integer,next_retry_at timestamptz,lease_token uuid?,lease_expires_at timestamptz?,received_at timestamptz,processed_at timestamptz?','consumer_name,event_id',['entry_id','consumer_name,aggregate_type,aggregate_id,aggregate_version'],[],'EVT-03/X-07; entry_id identifies one consumer/event row; sequence collision with a different hash is quarantined before overwrite.');
  add(`${ns}.consumer_stream_checkpoint`,'consumer_name text,aggregate_type text,aggregate_id text,last_sequence bigint,blocked_reason text?,updated_at timestamptz','consumer_name,aggregate_type,aggregate_id',[],[],'EVT-03; row lock with inbox business transaction.');
  add(`${ns}.event_stream_sequence`,'aggregate_type text,aggregate_id text,last_sequence bigint,version bigint','aggregate_type,aggregate_id',[],[],'EVT-01; allocate contiguous sequence with outbox insert.');
  alter(`${ns}.outbox_event`,'aggregate_id text,aggregate_version bigint,version bigint,payload_hash text,serialized_body bytea,lease_expires_at timestamptz?,event_version integer,producer text,correlation_id uuid,causation_id uuid,trace_id text','EVT-01/X-07; text aggregateId, immutable published bytes/hash, optimistic replay version. Existing lock_token/locked_at retained; legacy non-v1 rows archived before NOT NULL validation.');
  add(`${ns}.event_recovery_job`,'job_id uuid,entry_id uuid,queue_type text,consumer_name text?,event_id uuid,expected_hash text,source_version bigint,status text,request_hash text,idempotency_key text,requested_by_admin_id uuid,approval_id uuid?,lease_token uuid?,lease_expires_at timestamptz?,attempt_count integer,next_retry_at timestamptz,created_at timestamptz,completed_at timestamptz?,version bigint,last_error_code text?','job_id',['requested_by_admin_id,entry_id,idempotency_key'],[],'X-07/EVT-03; entry target is polymorphic inbox/outbox. Actor/approval IDs are verified by Commerce contract, no cross-service FK. Commit source retry and job state atomically; complete only after source effect/dedup confirmation.');
 }
 alter('commerce.member','session_version integer,nickname text,version integer','SEC-01/COM-01; PASSWORD credential authority is member_auth_identity. Legacy member.password_hash must be migrated/disabled, not dual-written.');
 alter('commerce.admin_user','password_hash text,totp_secret_cipher bytea?,mfa_state text,session_version integer,version integer','SEC-02/X-06; MFA recovery clears secret/codes and blocks privileged login until verified CLI enrollment.');
 add('commerce.admin_recovery_code','admin_user_id uuid,code_hash text,used_at timestamptz?','admin_user_id,code_hash',[],[['admin_user_id','commerce.admin_user','admin_user_id']],'SEC-02; one-time use conditional update.');
 add('commerce.verification_token','token_id uuid,member_id uuid,token_hash text,purpose text,expires_at timestamptz,used_at timestamptz?','token_id',['token_hash'],[['member_id','commerce.member','member_id']],'SEC-01; VERIFY_EMAIL/RESET_PASSWORD, never raw token.');
 add('commerce.seller_invitation','invitation_id uuid,seller_id uuid,email_normalized text,roles jsonb,token_hash text,expires_at timestamptz,accepted_member_id uuid?,status text','invitation_id',['token_hash'],[['seller_id','commerce.seller','seller_id'],['accepted_member_id','commerce.member','member_id']],'COM-02; 24h one-time invitation.');
 add('commerce.command_idempotency','principal_id text,operation text,target_id text,idempotency_key text,request_hash text,resource_id uuid?,result_payload jsonb,status text,created_at timestamptz,expires_at timestamptz?','principal_id,operation,target_id,idempotency_key',[],[],'API-02; command and result atomically persisted, no secrets.');
 add('commerce.claim_quote','quote_id uuid,order_id uuid,member_id uuid,order_version bigint,request_hash text,quote_snapshot jsonb,created_at timestamptz,expires_at timestamptz','quote_id',[],[['order_id','commerce.orders','order_id'],['member_id','commerce.member','member_id']],'X-01; 5 minute immutable quote, no stock/money reservation. Command idempotency replays same quote.');
 alter('commerce.claim_reason_change','version bigint,evidence_ids jsonb','X-02; evidence IDs only, owner verified; one open reason change per claim.');
 add('commerce.payment_dispatch_intent','order_id uuid,payment_request_id uuid,provider text,merchant_tx_id text,snapshot_hash text,gate text,attempt_id uuid?,status text,version bigint,created_at timestamptz','order_id',['payment_request_id'],[['order_id','commerce.orders','order_id']],'ORD-05; attempt_id is external Payment ID, no FK.');
 add('commerce.order_item_unit','order_item_id uuid,unit_ordinal integer,product_amount bigint,discount_amount bigint,paid_amount bigint,commission_amount bigint,status text,fulfillment_generation integer,version bigint','order_item_id,unit_ordinal',[],[['order_item_id','commerce.order_item','order_item_id']],'MONEY-01; ordinal 1..quantity. Sums match immutable order item.');
 add('commerce.shipment_item_unit','shipment_item_id uuid,order_item_id uuid,unit_ordinal integer,fulfillment_generation integer','shipment_item_id,order_item_id,unit_ordinal',[],[['shipment_item_id','commerce.shipment_item','shipment_item_id'],['order_item_id,unit_ordinal','commerce.order_item_unit','order_item_id,unit_ordinal']],'FUL-01; same order unit lock enforces only one active outbound per generation.');
 add('commerce.claim_item_unit','claim_item_id uuid,order_item_id uuid,unit_ordinal integer,refund_amount bigint,status text','claim_item_id,order_item_id,unit_ordinal',[],[['claim_item_id','commerce.claim_item','claim_item_id'],['order_item_id,unit_ordinal','commerce.order_item_unit','order_item_id,unit_ordinal']],'CLM-01; immutable allocation/history.');
 add('commerce.active_unit_claim_guard','order_item_id uuid,unit_ordinal integer,claim_item_id uuid','order_item_id,unit_ordinal',[],[['claim_item_id,order_item_id,unit_ordinal','commerce.claim_item_unit','claim_item_id,order_item_id,unit_ordinal']],'CLM-01; unique live claim per unit; terminal history remains in claim_item_unit.');
 add('commerce.purchase_confirmation_unit','order_item_id uuid,unit_ordinal integer,confirmation_event_id uuid,confirmed_at timestamptz','order_item_id,unit_ordinal',[],[['order_item_id,unit_ordinal','commerce.order_item_unit','order_item_id,unit_ordinal'],['confirmation_event_id','commerce.purchase_confirmation_event','confirmation_event_id']],'FUL-02; local confirmation event writer; no second income after exchange.');
 add('commerce.seller_financial_state','seller_id uuid,state_version bigint,account_version bigint,hold_version bigint,updated_at timestamptz','seller_id',[],[['seller_id','commerce.seller','seller_id']],'SET-03; barrier and fence and new hold share seller lock. Stream watermarks use event_stream_sequence.');
 add('commerce.payout_fence','fence_id uuid,seller_id uuid,settlement_id uuid,payout_attempt_id uuid,expected_seller_state_version bigint,stream_snapshot jsonb,account_snapshot_hash text,operation_hash text?,fence_version bigint,status text,expires_at timestamptz,consumed_at timestamptz?,dispatch_permit_id uuid?','fence_id',['dispatch_permit_id','payout_attempt_id,fence_version'],[['seller_id','commerce.seller','seller_id']],'SET-03; external settlement/payout IDs are not FKs. Same consumed fence replays same permit. Under seller lock expire the old unconsumed fence before creating a new version for the SAME payout attempt; partial unique payout_attempt_id WHERE status IN (ACTIVE,CONSUMED) required. Never create a new bank idempotency key to bypass UNKNOWN.');
 add('commerce.approval_execution','approval_request_id uuid,action_id uuid,payload_hash text,status text,operation_id uuid?,consumed_at timestamptz?','approval_request_id',['action_id'],[['approval_request_id','commerce.admin_approval_request','approval_request_id']],'OPS-01; 1-time execution matching approved immutable payload.');
 add('commerce.upload_asset','upload_id uuid,owner_type text,owner_id uuid,purpose text,storage_key text,content_type text,size_bytes bigint,sha256 text,status text,expires_at timestamptz,created_at timestamptz','upload_id',['storage_key'],[],'SEC-04; private evidence vs public image. Owner is polymorphic, checked in application.');
 alter('commerce.inventory_reservation','recovery_hold boolean,recovery_reason text?','ORD-05; expiry never overrides financial hold.');
 alter('commerce.orders','pricing_policy_version text,recovery_status text,payment_snapshot_hash text?','ORD-04; payment UNKNOWN is not order_status.');
 alter('commerce.admin_approval_request','payload_hash text,version bigint','OPS-01; EXPIRED already exists; map DB PENDING to HTTP APPROVAL_PENDING. Do not overwrite prior migration.');
 add('payment.order_payment_guard','order_id uuid,gate text,active_attempt_id uuid?,successful_payment_id uuid?,version bigint,closed_reason text?,updated_at timestamptz','order_id',[],[['active_attempt_id','payment.payment_attempt','payment_attempt_id'],['successful_payment_id','payment.payment','payment_id']],'PAY-01/03; CLOSED tombstone even before prepare; order_id external Commerce ID.');
 add('payment.payment_operation','operation_id uuid,order_id uuid,payment_attempt_id uuid?,payment_id uuid?,refund_id uuid?,kind text,status text,idempotency_key text,request_hash text,request_payload jsonb,result_version bigint,result_payload jsonb,provider text,merchant_tx_id text,lease_token uuid?,lease_version bigint,lease_expires_at timestamptz?,next_retry_at timestamptz,first_unknown_at timestamptz?,created_at timestamptz','operation_id',['provider,kind,merchant_tx_id,idempotency_key'],[['payment_attempt_id','payment.payment_attempt','payment_attempt_id'],['payment_id','payment.payment','payment_id'],['refund_id','payment.refund','refund_id']],'PAY-04; partial unique active dispatch per payment; due(status,next_retry_at). Payload may contain operation-local safe evidence only.');
 add('payment.payment_operation_result','operation_id uuid,result_version bigint,immutable_result jsonb,evidence_hash text,occurred_at timestamptz','operation_id,result_version',[],[['operation_id','payment.payment_operation','operation_id']],'PAY-02; immutable result, response and event reference identical version.');
 add('payment.refund_unit_allocation','refund_id uuid,order_item_id uuid,unit_ordinal integer,amount bigint','refund_id,order_item_id,unit_ordinal',[],[['refund_id','payment.refund','refund_id']],'PAY-04; order_item/unit external IDs validated against attempt snapshot; active/terminal successful unit reservation guard in owner transaction.');
 add('payment.pg_reconciliation_run','run_id uuid,provider text,business_date date,source_as_of timestamptz,algorithm_version text,status text,created_at timestamptz','run_id',['provider,business_date,source_as_of,algorithm_version'],[],'PAY-06; Payment owns PG reconciliation.');
 add('payment.pg_reconciliation_receipt','provider text,provider_transaction_id uuid,run_id uuid,receipt jsonb,receipt_hash text','provider,provider_transaction_id',[],[['run_id','payment.pg_reconciliation_run','run_id']],'PAY-06; immutable receipt; a later run can re-observe without inserting a second receipt.');
 add('payment.pg_reconciliation_discrepancy','discrepancy_id uuid,run_id uuid,identity_key text,kind text,status text,expected_payload jsonb,actual_payload jsonb,approval_id uuid?,resolved_at timestamptz?','discrepancy_id',['identity_key'],[['run_id','payment.pg_reconciliation_run','run_id']],'PAY-06; approval_id external Commerce identity.');
 alter('payment.payment_attempt','payment_request_id uuid,request_hash text,expires_at timestamptz,pricing_policy_version text','PAY-01; complete snapshot remains request_payload.');
 alter('payment.payment','voided_amount bigint,version bigint','PAY-04; VOID does not increment refunded_amount.');
 alter('payment.refund','compensation_id uuid?,approval_id uuid?,request_hash text','PAY-04; claimId or compensationId mandatory, approval for manual adjustment.');
 add('settlement.seller_recognition_unit','recognition_id uuid,seller_id uuid,order_id uuid,source_unit_or_charge_id text,source_event_id uuid,recognized_amount bigint,commission_amount bigint,reversed_amount bigint,reversed_fee_amount bigint','recognition_id',['seller_id,order_id,source_unit_or_charge_id'],[],'SET-01; snapshot/projection, no FK to Commerce.');
 add('settlement.seller_carry_forward','carry_forward_id uuid,seller_id uuid,currency text,source_settlement_id uuid,signed_amount bigint,target_settlement_id uuid?,source_ledger_ids jsonb,status text','carry_forward_id',['source_settlement_id'],[['source_settlement_id','settlement.settlement','settlement_id'],['target_settlement_id','settlement.settlement','settlement_id']],'SET-02; signed debt/credit, no duplicate allocation.');
 add('settlement.simulated_bank_receipt','bank_receipt_id uuid,idempotency_key text,dispatch_permit_id uuid,operation_hash text,account_snapshot_hash text,currency text,amount bigint,status text,occurred_at timestamptz','bank_receipt_id',['idempotency_key'],[],'SET-03; separate adapter transaction, no FK tying bank commit to caller commit.');
 alter('settlement.settlement','gross_net_amount bigint','MIG-NET/SET-02; API netAmount = gross_net_amount = credit - debit. Keep legacy net_amount = gross_net_amount - hold; payout bounded by post-hold net.');
 add('settlement.bank_reconciliation_run','run_id uuid,business_date date,source_as_of timestamptz,algorithm_version text,status text,created_at timestamptz,completed_at timestamptz?','run_id',['business_date,source_as_of,algorithm_version'],[],'MIG-BANK/SET-04; bank payout reconciliation only; fixed snapshot cursor and immutable run inputs.');
 add('settlement.bank_reconciliation_receipt','bank_receipt_id uuid,run_id uuid,idempotency_key text,account_snapshot_hash text,currency text,amount bigint,status text,occurred_at timestamptz,receipt jsonb,receipt_hash text','bank_receipt_id',['idempotency_key'],[['run_id','settlement.bank_reconciliation_run','run_id']],'MIG-BANK/SET-04; immutable observed bank fact. run_id is FIRST observation; later runs compare without updating original receipt. No FK to internal attempt: external-only receipts must be representable.');
 add('settlement.bank_reconciliation_discrepancy','discrepancy_id uuid,run_id uuid,identity_key text,bank_receipt_id uuid?,payout_attempt_id uuid?,kind text,status text,expected_payload jsonb,actual_payload jsonb,approval_id uuid?,resolution_event jsonb?,recorded_at timestamptz,resolved_at timestamptz?,version bigint','discrepancy_id',['identity_key'],[['run_id','settlement.bank_reconciliation_run','run_id'],['bank_receipt_id','settlement.bank_reconciliation_receipt','bank_receipt_id'],['payout_attempt_id','settlement.settlement_payout_attempt','payout_attempt_id']],'MIG-BANK/SET-04; deterministic identity excludes run ID, repeat observation reuses discrepancy; approval is external Commerce ID. Legacy reconciliation_* remain read-only.');
 alter('settlement.settlement_payout_attempt','approval_id uuid,fence_id uuid,dispatch_permit_id uuid?,operation_hash text,result_version bigint,lease_expires_at timestamptz?,next_retry_at timestamptz?','SET-03; external Commerce approvals/fence, no cross-service FK.');
 for(const ns of ['pg1','pg2']){
  add(`${ns}.pg_webhook_outbox`,'event_id uuid,transaction_id uuid,body jsonb,body_hash text,status text,attempt_count integer,next_retry_at timestamptz,lease_expires_at timestamptz?,created_at timestamptz','event_id',[],[['transaction_id',`${ns}.pg_transaction`,'transaction_id']],'PG-03; transaction commit and webhook row atomic; timestamp/signature refreshed on retry.');
  add(`${ns}.pg_fault_scenario`,'scenario_id uuid,scenario_type text,remaining_uses integer,delay_ms integer,expires_at timestamptz,enabled boolean','scenario_id',[],[],'PG-02; local/stage only; fail closed in prod profile.');
 }
 add('discovery.search_projection_generation','generation_id uuid,status text,snapshot_id uuid,watermarks jsonb,created_at timestamptz,activated_at timestamptz?','generation_id',[],[],'DIS-01; partial unique status ACTIVE; verified atomic generation switch.');
 add('discovery.search_product_projection','generation_id uuid,product_id uuid,revision_id uuid,source_version bigint,seller_id uuid,title text,category_id uuid,brand_id uuid?,min_price bigint,max_price bigint,available boolean,public_snapshot jsonb,published_at timestamptz','generation_id,product_id',[],[['generation_id','discovery.search_projection_generation','generation_id']],'DIS-01; all product/seller/revision IDs external, not FKs; query indexes on category/price/time+product_id.');
 alter('discovery.product_analysis_run','product_id uuid,simulated boolean,attempt_count integer,next_retry_at timestamptz?','DIS-02; externally owned product/revision IDs; no Commerce DB read.');
 applyTargetRules(m);
 validateModel(m);return m;
}
